A wallet holds key pair, derives its address from the public key, selects UTXO for payment, creates change and signs inputs. Blockchain addTransaction validates transactions againhs the UTXO set and pending mempool spends and then stores them in the mempool. Mining prepends a coinbas transaction into a block and updates the UTXO set and clears mined transactions
Each block stores transactions IDs in a Merkle root and mines by incrementing nonce until the hash meets the leading-zero requirement. Diffculty retargets every DIFFICULTY_ADJUSTMENT_INTERVAL blocks by comparing elapsed time with TARGET_BLOCK_TIME_MS
P2P nodes exchange sserialized chains and transactions over WebSocket. A node replace its chain only with a strictly longer, fully validates one. Accepted chains rebuild the UTXO set and clear stale mempool entries